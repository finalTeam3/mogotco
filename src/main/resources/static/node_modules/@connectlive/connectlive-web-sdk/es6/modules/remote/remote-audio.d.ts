import { RemoteParticipant } from "../participant/remote-participant";
import { ParticipantStream } from "../session/down-session";
import { AudioTrackType } from "../session/up-session";
import { Remote } from "./remote";
export declare class RemoteAudio extends Remote {
    type: AudioTrackType;
    extraValue: string;
    audioId: number;
    receiverId: string;
    isAlwaysOn?: boolean;
    active: boolean;
    constructor(stream: ParticipantStream, participant: RemoteParticipant);
    getAudioId(): number;
    _setReceiverId(receiverId: string): void;
    _setAlwaysOn(alwaysOn?: boolean): void;
    _setActive(active: boolean): void;
}
