import { Room } from '../room/room';
import { LocalAudio } from './local-audio';
import { LocalVideo } from './local-video';
declare class LocalScreen {
    room: Room | null;
    constraints: MediaStreamConstraints;
    audio: LocalAudio | undefined;
    video: LocalVideo | undefined;
    constructor(constraints?: MediaStreamConstraints);
    switchDisplay(): Promise<void>;
    _getDisplayMedia(): Promise<void>;
    stop(): void;
    _setRoom(room: Room): void;
}
export { LocalScreen };
