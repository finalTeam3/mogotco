import { HttpWithJSONRpc } from '../../utils/http-with-json-rpc';
export declare class AuthApiClient extends HttpWithJSONRpc {
    constructor();
    authorize(authConfig: AuthConfig, fingerprint?: string): Promise<AuthResponse>;
    refreshToken(serviceId: string, oldToken: string): Promise<RefreshTokenResponse>;
    private internally;
    private externally;
}
export declare type InternalAuthConfig = {
    endpoint?: string;
    serviceId: string;
    serviceSecret: string;
};
export declare type ExternalAuthConfig = {
    endpoint?: string;
    serviceId: string;
    token: string;
};
export declare type AuthConfig = InternalAuthConfig | ExternalAuthConfig;
export declare type AuthResponse = {
    uuid: string;
    token: string;
    ttl: number;
    api: string;
    cnl: string;
    fingerprint: string;
    iceServers: Array<{
        urls: string[];
        username?: string;
        credential?: string;
    }>;
};
declare type RefreshTokenResponse = {
    token: string;
    ttl: number;
};
export {};
