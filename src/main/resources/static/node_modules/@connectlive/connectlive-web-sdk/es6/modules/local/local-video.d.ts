import { VideoTrackType } from '../session/up-session';
import { Local } from './local';
export declare class LocalVideo extends Local {
    videoElements: HTMLVideoElement[];
    type: VideoTrackType;
    hd: boolean;
    cloneMediaStreamTracks: MediaStreamTrack[];
    constructor(mediaStreamTrack: MediaStreamTrack, type: VideoTrackType, extraValue?: string);
    attach(element?: HTMLVideoElement | undefined): HTMLVideoElement | undefined;
    detach(): void;
    stop(): void;
    setHd(enabled: boolean): void;
    _getCloneTrack(): MediaStreamTrack;
}
