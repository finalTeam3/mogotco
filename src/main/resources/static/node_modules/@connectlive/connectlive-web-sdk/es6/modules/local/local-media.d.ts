import { Room } from '../room/room';
import { LocalAudio } from './local-audio';
import { LocalVideo } from './local-video';
declare class LocalMedia {
    room: Room | null;
    constraints: MediaStreamConstraints;
    audio: LocalAudio | undefined;
    video: LocalVideo | undefined;
    constructor(constraints?: MediaStreamConstraints);
    getDevices(): Promise<MediaDeviceInfo[]>;
    getMicDevices(): Promise<MediaDeviceInfo[]>;
    getSpeakerDevices(): Promise<MediaDeviceInfo[]>;
    getCameraDevices(): Promise<MediaDeviceInfo[]>;
    switchCamera(deviceId: string): Promise<void>;
    switchMic(deviceId: string): Promise<void>;
    _getUserMedia(): Promise<void>;
    stop(): void;
    _setRoom(room: Room): void;
    setMediaStream(mediaStream: MediaStream): void;
}
export { LocalMedia };
