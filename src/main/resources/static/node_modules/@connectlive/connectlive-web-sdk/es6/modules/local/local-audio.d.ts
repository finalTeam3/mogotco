import { AudioTrackType } from '../session/up-session';
import { Local } from './local';
export declare class LocalAudio extends Local {
    type: AudioTrackType;
    isAlwaysOn: boolean;
    constructor(mediaStreamTrack: MediaStreamTrack, type: AudioTrackType, extraValue?: string);
    stop(): void;
    setAlwaysOn(enabled: boolean): Promise<void>;
}
