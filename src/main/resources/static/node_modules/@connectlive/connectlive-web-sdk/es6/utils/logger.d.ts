import { LogSender } from './log-send';
export declare const loggerSender: LogSender;
declare type LogLevelDesc = 'trace' | 'debug' | 'info' | 'error' | 'off';
export interface Logger {
    logLevelNumber: number;
    setLevel(logLevel: LogLevelDesc): void;
    trace(...msg: any[]): void;
    debug(...msg: any[]): void;
    info(...msg: any[]): void;
    error(...msg: any[]): void;
}
export declare const logger: Logger;
export declare const Loggable: (level: 'trace' | 'debug' | 'info' | 'error') => (target: any, propertyKey: string, descriptor: PropertyDescriptor) => PropertyDescriptor;
export {};
